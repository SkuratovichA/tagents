[10:50] the list stopped rearranging itself. Worktree `wt/list-order` on branch `feature/list-order`. Projects were sorted by the state of their most urgent agent, so a project climbed the list the moment one of its agents blocked and dropped back when it was answered; group order is now the open/closed bucket (`gmin[g] < prio["dead"]`) and then the path, so a project only ever moves when everything in it closes and it sinks to the dim section. Rows inside a project were sorted by state and then by age off the state record — which the hook rewrites on every tool call, so every working agent read `0:00` and two of them traded places while they ran. `hooks/tmux-agent-state.sh` now writes `$STATE_DIR/prompt/<key>` on `UserPromptSubmit` and on no other event (removed on SessionEnd and by `forget_agent`); `collect()` emits it as a `T` line, and `list()` takes both the row order (closed last, then most recently typed into) and the age column from it. The age column therefore answers "how long since my last message" — how much of the one-hour prompt cache is left — instead of "how long in this state"; sessions with no prompt file yet fall back to the record timestamp, and `DEAD_TTL` still measures the record, not the prompt. The group header stands in for the row under it, so its proxy member is picked on the row sort key rather than on urgency (`gmin` is kept, now only for the bold/dim and the bucket). README: new "The order, and the clock the rows are sorted on" section, the opening example reordered to match, the column legend now reads "since you last typed". Tests: `tests/ui.sh` §10/§10b (order held across a state change, recency after a prompt, header proxy, age from the prompt file, the no-file fallback) and `tests/launch.sh` §10 (the hook writes on the prompt alone and the record moves without it) — 14 suites green, 195 in ui.sh and 36 in launch.sh.
