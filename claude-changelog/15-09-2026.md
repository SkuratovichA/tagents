# 15-09-2026

[13:05] tnotes: closing the editor a second time re-typed the artifact mentions. The dedup read only the cursor's row of the chat pane, but in a sidebar-wide pane the `@…/doc.md` mentions wrap onto several rows, so every mention above the last row was missed and typed again on each :q. `input_text()` now reads the whole input box — the rows between the ─ rule above the cursor and the one below, as the CLI draws it (a pane with no rule gets the non-blank rows ending at the cursor) — and the rows are also compared joined, for a mention the wrap split mid-word. tests/notes.sh 6c pins it with a 48-column chat: 82 passed, and 3 of the new 5 fail on the previous tnotes.

[14:51] fixed the headless driver's argv for fresh sessions (packages/core/src/claude-driver.ts, buildArgs): `--disallowedTools` and `--strict-mcp-config --mcp-config` now come right after `--model`/`--effort` and before `--output-format`, instead of after the system prompt. Both are variadic on the CLI, and with no `--resume` between them and the prompt claude took the prompt as a tool name and exited 1 with "Input must be provided either through stdin or as a prompt argument" — the busano ticket agent failed every batch since the 01:50 cutover (two dead letters, 13:21 and 14:10). Updated the documented-order test in test/argv-env.test.ts and added a regression test that walks several specs × {fresh, resumed} and asserts a variadic flag is always followed by one value and then an option. Verified on the real CLI (`claude -p --model haiku --disallowedTools Agent,Task --output-format text 'reply with exactly OK'` → OK; the old order reproduces the error). `pnpm build && pnpm typecheck && pnpm test` in packages/core: 138/138. dist rebuilt, so consumers on the workspace link pick it up on restart.
