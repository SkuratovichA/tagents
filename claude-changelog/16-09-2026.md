[11:35] the month is the meter. `tusage --meter` fetches `GET /api/oauth/usage` with the account's own OAuth token (macOS keychain item `Claude Code-credentials` for the default login; `TU_METER_ITEMS` names others), writes `usage/meter.tsv` (used, limit, fetched-at, status, next-try), and recalibrates `factor.tsv` from the reading so the per-day rows sum to it. `tusage --update` — which the dashboard's refresher already runs — refreshes it outside the index lock, at most every `TU_METER_TTL` seconds (900; 300 above 90 % of the limit), with `nometer` for a plan seat (24h) and `error` (5 min, last good reading kept). The dashboard takes the limit and the `~meter HH:MM` label from that file while the reading is under 48h old; `usage.monthly_limit_usd` and the hand-typed `usage.meter` leaf are the fallback and the override. Why: the transcript estimate was $1108 against a meter of $830, wrong per session in both directions against Claude Code's own ledger, and its hand factor drifted from 0.98 to 0.73 in two days. Suites: tusage 62, usage 41, config 103, ui 186 — 0 failed. Landed 4e41998..2483ee2. One defect caught in review: the first commit had dropped the executable bit on `tusage`; folded back before landing.
[11:50] the refresher's `tusage --update` ran without `usage_env`, so every 5th tick rebuilt the account map under config-dir labels (`default`, `d`), the next dashboard read rebuilt it back under the profile names, and — since the meter — the reading was fetched a second time under `default`. One `usage_env` before the loop; `tests/usage.sh` 5 now drives `--refresher` against a dead port and asserts the `--update` call carries `TU_ACCOUNTS` (fails on the previous refresh.sh). usage 42, ui 186, 0 failed.
