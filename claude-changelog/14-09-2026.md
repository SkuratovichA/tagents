# 14-09-2026

[23:30] core 0.3.1: `declare module 'i18next'` removed from packages/core/src/i18n/index.ts.
Core's keys are typed locally instead — `CoreKey`, `CoreVars`, `CoreT` and `coreT(t)`,
a key-checked view over i18next's TFunction. cli/plugin.ts, cli/plugin-run.ts and
cli/main.ts take `CoreT` (main wraps once: `coreT(createT())`); `PluginContext.t`
stays i18next's permissive TFunction, because plugins have their own keys. Found by
the first clean clone of the tg-orchestrator export: two type errors in its
test/i18n.test.ts that never appeared locally, since a link: dependency keeps two
i18next copies (core's under typescript@5.9.3, the plugin's under 7.0.2) and an
augmentation only reaches the module it resolves to. In a real install both resolve
to one file and core's `defaultNS: 'core'` retyped the plugin's t() to core's keys.
Gates: core 131/131 (one 400 ms timing test failed once with eleven claude
processes on the box, green on both re-runs), knowledge 52/52, orchestrator 204/204
and typecheck through the link, then the published export's clean clone.

[23:05] Effect trial started for the driver lifecycle, on the nuzhin account as
headless agents: spike/effect-driver (lifecycle on effect@3.22.2), spike/plain-driver
(same lifecycle restructured with no dependency, injectable clock) and, in the
orchestrator, spike/effect-plugin (proposal with compiling code). Identical
measurement protocol in both driver spikes; verdict pending their reports.

[00:40] Verdict on Effect for the driver, from the two spikes (branches
spike/effect-driver and spike/plain-driver kept locally, unpushed; reports in
the session scratchpad). The Effect version costs +49 ms on every CLI start
(77 → 126 ms), +15.5 MB RSS after import (61 → 76.5), 37.5 MB / 3741 files of
runtime dependencies (fast-check among them) and +28% source, for two gains —
scope-tied cleanup and virtual-time tests — that the dependency-free restructure
delivers at zero cost. Both agents recommended against; the numbers were
re-measured here (effect/Effect import 49–78 ms and 62 MB; CLI --help 122 vs
71 ms). Merged the restructure as core 0.3.2 (00a8c70, b256bb5): a Phase union
(running | resulted | exited), one TurnScope owning every timer and the abort
listener (dispose() doubles as the done flag), an injectable clock
(ClaudeDriverOptions.clock), and five lifecycle tests on a fake clock that pin
the production 50-minute / 45-minute / 60-second limits to the millisecond. It
closes two latent leaks: a result flushed by reader.end() inside finish()
re-armed the 60 s grace timer after cleanup, and an exit after error scheduled
a 5 s drain timer. Gates: core 136/136 (one real-timer test red once under
load, green on re-run), knowledge 52/52, tests/headless.sh 58/58, orchestrator
204/204 through the link. Noted, not done: the killer is not cancelled on exit
(a process leaving in the last pipeDrainMs before the deadline reports
timeout), `running` is keyed by ref, and turn.test.ts still runs real 400 ms
timers. The .dotfiles submodule points at b256bb5 (135353a).
