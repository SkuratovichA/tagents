{
  "name": "tagents-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "tmux dashboard for Claude Code sessions (bash) plus the TypeScript packages that drive headless ones",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
