[00:30] Split `tagents` (5473 lines) into an entry point and lib/tagents/*.sh, moving code verbatim. The entry keeps the four things that cannot move — the manual (`--help` prints it out of $SELF), SELF itself, `forward_core` and the dispatch `case` — and is 573 lines, 410 of them the manual. The other 19 files are one per part: core, config, accounts, usage, state, names, columns, list, timeline, preview, actions, refresh, sidebar, seats, dock, closed, launch, keys, dash. Nothing runs at source time, so the load order is reading order; core.sh is first because the other constants are built on STATE_DIR and TAB.
[00:30] How it was done and checked, since "it still passes" is not evidence for a move of this size: the split is mechanical (a manifest of function -> module; every one of the 5473 lines asserted to land in exactly one place, comments travelling with the code they explain); bash's own view is the oracle — sourcing the definitions before and after and diffing `declare -f` and `declare -p` under bash 3.2 gives byte-identical function bodies and globals; every read-only mode (--list, --flat, --counts, --timeline, --keys, --header, --col-rows, --closed-rows, --seats, --config, --help) was run against the live state directory before and after and diffed byte for byte; 13 suites, 781 checks, green; timings unchanged within noise (--keys 150 vs 156 ms, --counts 434 vs 438, --list 400 vs 390).
[00:30] The lookup: SELF stays the path the program was invoked by (fzf bindings, tmux run-shell and ${SELF%/*}/tnotes all depend on it), while the modules are found beside the REAL file by following symlinks by hand — macOS has no readlink -f, and the install is ~/.local/bin/tagents -> the .dotfiles submodule. Nothing is loaded for --help, so a half-copied checkout can still say what it is; a missing module is a one-line refusal naming the directory, not a `command not found` deep inside an fzf child.
[00:30] Tests: tests/dispatch.sh copies lib/ along with the script into its fixtures; tests/ui.sh's fzf-stderr scan now covers every module (per-file window, FILENAME:FNR in the message); new tests/modules.sh (12 checks) guards the layout itself — every module sourced, no orphans, nothing defined twice, modules define and never run, every dispatch verb still has its function, and the entry works through a symlink and a symlink-to-a-symlink but refuses to run as a lonely copy.
[00:30] Docs: new lib/tagents/README.md (the module table plus how the parts fit — where the facts come from, what reads them, which processes run what); README gains the lib/tagents/ row, the "this is an entry point, not a file to copy" note under Install, and the tests paragraph; tnotes lost a stale `(tagents:1169)` pointer.
[00:30] From the review, two functions moved to where their only callers are: pane_exists to dock.sh and keyhdr to dash.sh. install_focus_hooks stays in refresh.sh deliberately — it installs the tmux hooks that fire --poke and --unpark, which live there; dock only makes sure they exist.
[08:51] Merged origin/feature/usage-meter (3 commits, written before the split) onto the module layout as a real merge (5cf975c). `tagents` resolved to main's version; the branch's hunks there — usage_days' optional --until, usage_calibration with its essay, and usage_line's factor/label/note split, the per[]/mtd scaling at the door, the ~ in the status segment and the footer's "~meter <reading>" — were re-targeted onto lib/tagents/usage.sh by rewriting the patch's path (all five hunks applied at a uniform -958 offset, i.e. the context matched exactly). Proof of the move: usage_days..usage_line extracted from the branch's tagents and from the module are 164 lines each with an empty diff, and declare -f of the three functions is byte-identical.
[08:51] README.md auto-merged with both sides (main's layout notes, the branch's "indexed by session" and "the meter calibrates the month" paragraphs under tusage). config.example.yaml and tests/config.sh auto-merged too: main had added to both since the merge-base, the result is main's additions plus the branch's usage.meter lines. tests/usage.sh keeps its executable bit, which the branch's version had dropped. Suites: 779 passed / 2 failed before, 790 / 1 after — the 10 new meter cases and modules.sh green. The one red is pre-existing on main: tests/ui.sh "no config: the rows are what they were before any of this" runs `git show HEAD:tagents` as a lonely copy, which the split entry refuses to run; names.sh (b) failed once before the merge and passed after, a tmux timing flake. Left alone: lib/tagents/README.md's table still says usage.sh is 218 lines (273 now) — the other agent's module edits move those counts too.
