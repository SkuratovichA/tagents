[13:10] tagents comes back after a reboot. Worktree `wt/resurrect` on branch `feature/resurrect`. Every tmux-resurrect save (`prefix C-s` and continuum's timer, through `@resurrect-hook-post-save-layout`) now also runs `tagents --resurrect-save`, which writes `$STATE_DIR/resurrect/<epoch>.tsv`: one `A` row per live chat pane (session id, config dir, whether the record carried one, tmux session/window/pane, window name and tagents name, cwd, docked, whether its tnotes editor was on screen, model, and the argv's `--model`/`--effort`) plus a `D` row naming the dash window; the rows are sorted by placement and deduped against the previous file with `cmp -s`, the temp file comes from `mktemp`, and older files are pruned to `RESURRECT_KEEP` except the one a restore would pick. After tmux-resurrect's post-restore-all hook, `tagents --resurrect --auto` picks the newest snapshot written strictly before this server's `#{start_time}` (a capture that lands in the server's first second is the empty post-boot layout and must not shadow the pre-reboot set), takes the mkdir lock, and resumes each chat where it was: `respawn-pane -k` into the restored pane, else `new-window` at the recorded index, else `new-session`; on the login it ran on through `resume_profile`/`agent_cmd`, the pane removing its own stale record before `exec`ing claude; a chat whose transcript is gone is skipped with a note, a `hascfg=0` chat is checked under the default config dir. `ensure_dash` adopts the stale `dash` window tmux-resurrect brought back, and `resurrect_notes` closes the editor shells restored into a chat's window and runs `tnotes toggle` once per chat that had its editor on screen. `--auto` runs once per server (`.resurrect.done` holds the start_time) and only within `RESURRECT_YOUNG` (300 s); captures are held back until then and while a restore holds the lock, and a restore retakes the snapshot 15 s after it finishes. Captures also fire 5 s after every launch and kill (`resurrect_soon`, immune to the HUP the closing popup sends). Config `resurrect.auto` (true), `resurrect.every` (300, `TA_RESURRECT_EVERY` wins, 0 off), `resurrect.notes` (reopen | off); verbs `--resurrect-save`, `--resurrect-rows [file|latest]`, `--resurrect [--dry-run] [--auto] [--from F|latest]`; the report goes to `$STATE_DIR/resurrect.log`, trimmed to 2000 lines. Along the way `lock_dir` (core.sh) became bounded: a stat failure no longer reads as age-from-epoch and every pass costs a try; `adopt_dash_window` leaves a dash window whose list still runs alone, and `ensure_dash` waits for a list it just started on all three paths. `.tmux.conf` gains the two hook lines. Tests: new `tests/resurrect.sh` (193 checks: rows, the pick, placement on all three paths, profiles, transcript gone, dry-run, the gates, lock bounds, prune, notes on screen / parked / orphans, adoption in one run, retake after a launch and a kill); 16 suites green.
